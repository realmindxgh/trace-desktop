name: Trace Windows native build (legacy bootstrap)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-bootstrap:
    runs-on: windows-latest
    steps:
      - name: Legacy bootstrap retired
        run: |
          echo "The original source-transfer bootstrap is retained for history only."
          echo "Use windows-v2-main.yml for Windows release candidate builds."
